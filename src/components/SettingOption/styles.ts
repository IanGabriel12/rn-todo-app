import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  optionContainer: {
    marginTop: 15,
    flexDirection: "row"
  },

  optionName: {
    fontSize: 20,
    marginLeft: 20,
  }
})

export default styles;